kind = "web"
previewPath = "/"
title = "FrameSnap"
version = "1.0.0"
id = "artifacts/framesnap"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21183

[services.development]
run = "pnpm --filter @workspace/framesnap run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/framesnap", "run", "build" ]
publicDir = "artifacts/framesnap/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21183"
BASE_PATH = "/"
